# ACEest Fitness and Gym - Flask Application

A foundational Flask web application for managing workouts.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the application:
   ```bash
   python run.py
   ```

3. Open in browser:
   ```
   http://127.0.0.1:5000
   ```
